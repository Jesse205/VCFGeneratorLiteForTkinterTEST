[tool.poetry]
name = "vcf_generator"
# VERSION
version = "3.0.0"
# END-VERSION
description = "A simple VCF generator"
authors = ["Jesse205 <2055675594@qq.com>"]
license = "MIT"
readme = ["README.md", "README.zh.md"]
package-mode = false
repository = "https://gitee.com/HelloTool/VCFGeneratorLiteForTkinter"
packages = [
    { include = "vcf_generator" },
    { include = "scripts" },
]

[tool.poetry.scripts]
build-app = "scripts.buildApp:main"
build-setup = "scripts.buildSetup:main"
change-version = "scripts.changeVersion:main"
generate-version-file = "scripts.generateVersionFile:main"

[tool.poetry.dependencies]
python = ">=3.8,<3.13"
pyinstaller = "^6.8.0"
pyinstaller-versionfile = "^2.1.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

#requires = ["setuptools>=42", "wheel", "poetry-core", "nuitka", "toml"]
#build-backend = "nuitka.distutils.Build"